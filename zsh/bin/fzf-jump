#!/bin/zsh

j() {
  fd --type d --no-hidden --max-depth 7 |
    fzf --preview 'tree -C {} | head -50' |
    read dir

  kill $(jobs -p) 2>/dev/null

  [[ -n "$dir" ]] && cd "$dir"
}

# Add alias for easier access (optional)
alias jj='j'
