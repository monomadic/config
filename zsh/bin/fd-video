#!/bin/zsh

# Search for video files with specific extensions, case-insensitive
fd --absolute-path \
  --color=never \
  --ignore-case \
  --type=file \
  --exclude '/Volumes/*Backup/**' \
  --extension mp4 \
  --extension mkv \
  --extension mov \
  --extension webm \
  --extension m4v \
  "$@"
