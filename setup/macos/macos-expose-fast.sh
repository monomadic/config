#!/usr/bin/env bash

# expose animation speed
defaults write com.apple.dock expose-animation-duration -float 0.1

# disable animations
# defaults write com.apple.dock expose-animation-duration -float 0
